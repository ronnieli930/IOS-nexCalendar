import SwiftUI

struct CourseIntroMain: View {
    var body: some View {
        VStack {
            Text("useless")
            
        }
        //
                        
        //                    .padding(.top, 20)
        //            }.foregroundColor(Color.white.opacity(0.3))
    }
}

struct CourseIntroMain_Previews: PreviewProvider {
    static var previews: some View {
        CourseIntroMain()
    }
}
